# Generated by blcgen — DO NOT EDIT BY HAND.
# Source: network/config/network.yaml + network/deployment/local.yaml
# Regenerate with: blcgen generate compose

services:
  ca.{{.Orderer.Name}}:
    image: hyperledger/fabric-ca:{{.CAVersion}}
    container_name: ca.{{.Orderer.Name}}
    environment:
      - FABRIC_CA_SERVER_HOME=/etc/hyperledger/fabric-ca-server
      - FABRIC_CA_SERVER_TLS_ENABLED=false
      - FABRIC_CA_SERVER_CA_NAME=ca-{{.Orderer.Name}}
    ports:
      - "{{.Orderer.CAPort}}:7054"
    volumes:
      - ../crypto/ca-servers/{{.Orderer.Name}}:/etc/hyperledger/fabric-ca-server
    command: sh -c 'fabric-ca-server start -b admin:adminpw'
    networks:
      - blc
{{range .ActiveOrgs}}
  ca.{{.Name}}:
    image: hyperledger/fabric-ca:{{$.CAVersion}}
    container_name: ca.{{.Name}}
    environment:
      - FABRIC_CA_SERVER_HOME=/etc/hyperledger/fabric-ca-server
      - FABRIC_CA_SERVER_TLS_ENABLED=false
      - FABRIC_CA_SERVER_CA_NAME=ca-{{.Name}}
    ports:
      - "{{.CAPort}}:7054"
    volumes:
      - ../crypto/ca-servers/{{.Name}}:/etc/hyperledger/fabric-ca-server
    command: sh -c 'fabric-ca-server start -b admin:adminpw'
    networks:
      - blc
{{end}}
networks:
  blc:
    name: blc
