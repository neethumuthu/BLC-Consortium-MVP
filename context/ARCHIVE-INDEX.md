# Archive Index

Index of legacy documents processed by the `doc-archaeology` pass
(2026-07-30). Source corpus lived outside this repo, at
`/home/neethu/Desktop/ESPEO/Projects/BLC-Consortium-V1-docs-archive/`
(never committed here — may contain internal-only planning detail). Each
row: what the doc covered, and the verdict after cross-checking its claims
against the real `network/`, `chaincode/`, `backend/`, and `frontend/` code.

| Title | Location (archive path) | What it covered | Verdict |
|---|---|---|---|
| PPT_PROMPT.md | `BLC-Consortium-V1-docs-archive/PPT_PROMPT.md` | Slide-deck authoring brief for a 10-12 slide internal "BLC-31 v1.0 architecture" presentation: two-pipeline model, network topology, identity/crypto model, chaincode architecture, certificate data model/lifecycle, live institution onboarding, backend/API layer, what's proven vs. built, and v1.0 scope boundaries. | Distilled, with conflicts. The vast majority of architecture/topology/chaincode claims were confirmed directly against code. Two claims are now stale: the "no login/API keys yet" auth-scope statement (Slide 10/12) and the framing of InstitutionB as still-applying (Slide 2/9) — both superseded by later commits. See `context/CONFLICTS.md`. |
| TEAM_DEMO_PREP.md | `BLC-Consortium-V1-docs-archive/TEAM_DEMO_PREP.md` | Runbook for the 2026-07-24 full-system team demo: pre-demo network/chaincode setup, live governance-vote + `org-add.sh` onboarding of InstitutionB, Swagger-UI-driven certificate issuance/verification/revocation walkthrough, cross-org rejection proof, and troubleshooting notes. | Distilled, with conflicts. Command-level details (CC_VERSION/CC_SEQUENCE values, peer-join retry behavior, port numbers, env var names) were confirmed byte-for-byte against `network/scripts/chaincode.sh`, `network/scripts/org-add.sh`, and `network/scripts/network.sh`. As a runbook it is now event-specific history — InstitutionB (the demo's centerpiece target) is already a permanent member as of commit `abb1e3d`, and the backend now has API-key auth this doc's talking points describe as absent. Re-running its setup steps as-is against the current repo would be incorrect. |
| V1_PHASE_OVERVIEW.md | `BLC-Consortium-V1-docs-archive/V1_PHASE_OVERVIEW.md` | Comprehensive "everything built in v1.0" reference (compiled from `docs/BUILD_LOG.md`, `ARCHITECTURE.md`, `docs/ERROR_LOG.md`): two-pipeline architecture in depth, full network/identity/chaincode/backend detail, work-by-theme summary, scale-of-verification claims (incident/phase/test counts), and explicit out-of-scope items. | Distilled, with conflicts. Nearly every checkable architecture/code-behavior claim (chaincode function signatures and counts, ledger asset fields, endorsement policy, NodeOUs, crypto disk layout, backend connection model, error-mapping regexes, retry logic, the deterministic-TxID-not-UUID design choice, and the 6-voter close-race governance test) was confirmed directly against source and merged into `context/codebase/{STACK,CONVENTIONS,CONCERNS,TESTING}.md` with `doc-derived` provenance notes. The same two stale claims as the other two docs (no-auth, InstitutionB-pending) are logged in `context/CONFLICTS.md`. A handful of historical/process statistics (36 dated incidents, 11 phases, "7 real infrastructure bugs") are not code-checkable and have no home in this repo's `context/` yet — see the calling agent's report for the full breakdown, including one incident (an over-broad cleanup command destroying a channel-committed org's CA identity) flagged as a candidate for a retroactive ADR. |
