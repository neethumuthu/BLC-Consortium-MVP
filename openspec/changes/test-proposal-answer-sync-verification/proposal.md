## Why

Temporary verification artifact — not a real feature. Exists only to give `requirements-nudge.yml` an open question to raise and `proposal-answer-sync.yml` a real PM-answer round trip to resolve, so both can be tested against the live GitHub Actions runner.

## What Changes

- Nothing real. This change is removed once the round trip is confirmed.

## Capabilities

### New Capabilities
(none — verification only)

### Modified Capabilities
(none)

## Open questions

- Resolved via issue #6: answered "yes".

## Impact

- None — this folder is deleted once the automated PR round trip is confirmed.
