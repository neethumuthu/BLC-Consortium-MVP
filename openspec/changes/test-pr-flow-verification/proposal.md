## Why

Temporary — verifying project-sync.sh's PR-triggered Stage transitions (Verify on open, Compound on merge) end to end against a real Project item. Not a real change.

## What Changes

Nothing real. This folder exists only to exercise the pull_request-triggered code path that project-sync.sh's push-triggered path never touches.
