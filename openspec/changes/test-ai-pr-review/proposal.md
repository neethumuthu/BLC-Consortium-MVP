## Why

Temporary — verifying ai-pr-review.yml's structured Ring 2 review actually runs and posts real review comments now that CLAUDE_CODE_OAUTH_TOKEN works. Not a real change.

## What Changes

Nothing real. This folder + PR exist only to exercise the pull_request-triggered review path.
